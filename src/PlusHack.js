import React from "react";
import { Button} from "react-bootstrap";

const PlusHack = () =>{
    <Button variant="primary" href="http://localhost:3000" className='mt-4'>
            Вернуться на главную
    </Button>
}

export default PlusHack; 